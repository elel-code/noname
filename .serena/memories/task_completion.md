# 任务完成提醒
- 运行 `python main.py` 确保没有运行错误，输出 `Hello from noname!`。
- 检查 `git status` 确认仅包含预期文件更改；若新增依赖，更新 `pyproject.toml` 并同步 `pip freeze > requirements.txt`（若需要）。
- 若创建新模块，编写对应单元测试（暂无测试框架，但可以扩展）。
- 提交前执行 `git diff` 和 `git show` 以验证变更，必要时更新 `README.md` 和 `AGENTS.md`。